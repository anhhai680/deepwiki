# Docker-specific .env configuration
# Use this file when running DeepWiki in Docker containers

# Copy this file to .env when using Docker

# OpenAI API Key (required for embeddings even if using other models)
OPENAI_API_KEY=your_openai_api_key

# Optional API Keys
GOOGLE_API_KEY=your_google_api_key
OPENROUTER_API_KEY=your_openrouter_api_key

# Server configuration
SERVER_BASE_URL=http://localhost:8001
PORT=8001

# GitHub token (optional, for private repos)
GITHUB_TOKEN=

# Ollama configuration for Docker
# Use host.docker.internal to access Ollama running on the host machine
OLLAMA_HOST=http://host.docker.internal:11434

# For Linux Docker (if host.docker.internal doesn't work)
# OLLAMA_HOST=http://172.17.0.1:11434

# Logging
LOG_LEVEL=INFO
LOG_FILE_PATH=backend/logs/application.log
